<h1><%= @article.title %></h1>

<div>
  <p><%= simple_format(@article.content) %></p>
</div>

<small>作成日: <%= @article.created_at.strftime("%Y年%m月%d日 %H:%M") %></small>
<small>更新日: <%= @article.updated_at.strftime("%Y年%m月%d日 %H:%M") %></small>

<p>
  <%= link_to "記事一覧に戻る", articles_path %>
</p>
